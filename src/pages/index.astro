---
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/Section.astro";
import FeatureGrid from "../components/FeatureGrid.astro";

import romanPhoto from "../images/Roman.jpeg";
import kristyPhoto from "../images/Kristy.jpeg";

const baseUrl = import.meta.env.BASE_URL;
const withBase = (path: string) => `${baseUrl.replace(/\/$/, "")}/${path.replace(/^\//, "")}`;

const title = "Исследование интеграции ИИ и АСУ в компании — Лаборатория ИИ СКОЛКОВО";
const description =
  "Масштабное исследование от Лаборатории ИИ Школы управления СКОЛКОВО. Участвуйте в опросе для аптечных сетей, добывающих и инвестиционных компаний.";
const siteUrl = "https://pemuul.github.io/lend_my_and_kristy";
const canonical = `${siteUrl}/`;
const ogImage = `${siteUrl}/og/og-home.svg`;

const aiSpheres = [
  { title: "Маркетинг и продажи", description: "Автоматизация рекламы, генерация контента, сегментация.", icon: "lucide:megaphone" },
  { title: "Клиентский сервис", description: "Чат-боты, обработка запросов 24/7.", icon: "lucide:headphones" },
  { title: "Аналитика и прогнозирование", description: "Big Data, исследование рынка, предиктивные модели.", icon: "lucide:bar-chart-3" },
  { title: "HR и коммуникации", description: "Автоматизация найма, внутренние ассистенты.", icon: "lucide:users" },
  { title: "Документооборот", description: "Ускорение обработки, повышение прозрачности.", icon: "lucide:file-text" },
  { title: "Финансы и бухгалтерия", description: "Анализ транзакций, ускорение отчетности.", icon: "lucide:calculator" },
];
---
<BaseLayout title={title} description={description} canonical={canonical} ogImage={ogImage} accent="blue">
  <!-- Hero -->
  <section class="hero-grid relative flex min-h-[85vh] items-center overflow-hidden bg-mesh pt-4 sm:min-h-[80vh] sm:pt-6">
    <div class="glow-orb glow-orb-ambient top-0 left-1/4 h-[480px] w-[480px] bg-blue/25" aria-hidden="true" style="animation-delay: 0s;"></div>
    <div class="glow-orb glow-orb-ambient top-1/3 right-0 h-[360px] w-[360px] bg-violet/20" aria-hidden="true" style="animation-delay: -3.3s;"></div>
    <div class="glow-orb glow-orb-ambient bottom-1/4 left-1/2 h-[280px] w-[280px] bg-cyan/15" aria-hidden="true" style="animation-delay: -6.6s;"></div>
    <div class="hero-scan-line" aria-hidden="true"></div>
    <div class="container-wide relative z-10 py-12">
      <div
        class="pattern-bg absolute right-0 top-0 h-full w-full"
        style={`background-image: url('${withBase("/patterns/circuit-blue.svg")}');`}
        aria-hidden="true"
      ></div>
      <div class="relative z-10 max-w-4xl">
        <span class="badge reveal">Февраль 2026 · Три отрасли</span>
        <h1 class="hero-title mt-6 text-4xl font-bold leading-tight tracking-tight text-text reveal sm:text-5xl lg:text-6xl">
          Масштабное исследование от Лаборатории ИИ Школы управления СКОЛКОВО
        </h1>
        <p class="mt-6 text-xl font-medium text-muted reveal sm:text-2xl">
          Интеграция ИИ и автоматизированных систем управления (АСУ) в современный бизнес
        </p>
        <p class="mt-3 text-sm font-medium uppercase tracking-widest text-muted reveal">
          Фокус февраля: аптечные сети, добывающие компании, частные инвестиционные организации
        </p>
        <p class="mt-8 max-w-2xl text-base leading-relaxed text-muted reveal sm:text-lg">
          Мы изучаем, как российские компании внедряют нейросети, чтобы выявить лучшие практики, барьеры и создать карту возможностей.
        </p>
      </div>
    </div>
  </section>

  <!-- Сферы ИИ -->
  <Section
    id="sfery"
    overline="Сферы применения"
    title="Ключевые сферы применения нейросетей в российском бизнесе"
    subtitle="От маркетинга до финансов — области, где ИИ уже меняет правила игры."
    alt
  >
    <FeatureGrid items={aiSpheres} />
  </Section>

  <!-- Участие и опросники -->
  <section id="uchastie" class="section-spacing">
    <div class="container-tight">
      <div class="reveal">
        <p class="overline mb-3">Участие в исследовании</p>
        <h2 class="text-2xl font-bold tracking-tight text-text sm:text-3xl lg:text-4xl">
          Примите участие в исследовании для вашей отрасли
        </h2>
        <p class="mt-4 max-w-2xl text-base text-muted sm:text-lg">
          Заполните специализированный опросник за 10–15 минут. Ваши ответы — ценный вклад в объективную картину. В благодарность — бесплатная 30-минутная консультация от экспертов лаборатории по потенциалу интеграции ИИ в вашу компанию.
        </p>
        <div class="mt-10 grid gap-5 sm:grid-cols-1 md:grid-cols-3">
          <a
            href={withBase("survey-pharmacy")}
            class="survey-card-pharmacy card-surface card-surface-interactive split-panel flex min-h-[140px] flex-col justify-between p-6 text-left transition focus-ring"
          >
            <div>
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-xl bg-emerald/20 text-emerald">
                <span class="text-lg" aria-hidden="true">💊</span>
              </span>
              <h3 class="mt-4 text-lg font-semibold text-text">Аптечные сети и Фармацевтика</h3>
              <p class="mt-2 text-sm text-muted">Учёт, рецепты, ОМС, 152-ФЗ</p>
            </div>
            <span class="mt-4 inline-flex items-center gap-2 text-sm font-medium text-emerald">Перейти к опроснику →</span>
          </a>
          <a
            href={withBase("survey-mining")}
            class="survey-card-mining card-surface card-surface-interactive split-panel flex min-h-[140px] flex-col justify-between p-6 text-left transition focus-ring"
          >
            <div>
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-xl bg-amber/20 text-amber">
                <span class="text-lg" aria-hidden="true">⛏</span>
              </span>
              <h3 class="mt-4 text-lg font-semibold text-text">Добывающие и Промышленные компании</h3>
              <p class="mt-2 text-sm text-muted">Оборудование, геологоразведка, IoT, АСУ ТП</p>
            </div>
            <span class="mt-4 inline-flex items-center gap-2 text-sm font-medium text-amber">Перейти к опроснику →</span>
          </a>
          <a
            href={withBase("survey-investment")}
            class="survey-card-investment card-surface card-surface-interactive split-panel flex min-h-[140px] flex-col justify-between p-6 text-left transition focus-ring"
          >
            <div>
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-xl bg-blue/20 text-blue">
                <span class="text-lg" aria-hidden="true">📈</span>
              </span>
              <h3 class="mt-4 text-lg font-semibold text-text">Инвестиционные и Финансовые организации</h3>
              <p class="mt-2 text-sm text-muted">Due diligence, портфели, отчётность, регуляторы</p>
            </div>
            <span class="mt-4 inline-flex items-center gap-2 text-sm font-medium text-blue">Перейти к опроснику →</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Результаты исследования: счётчик -->
  <section id="research-results" class="section-spacing" aria-labelledby="research-results-title">
    <div class="container-tight">
      <div class="research-counter-card reveal">
        <div class="research-counter-glow" aria-hidden="true"></div>
        <p class="overline mb-3">Результаты исследования</p>
        <h2 id="research-results-title" class="text-2xl font-bold tracking-tight text-text sm:text-3xl">
          Уже участвуют в исследовании
        </h2>
        <p class="mt-2 text-base text-muted sm:text-lg">
          Количество заполнивших опросник продолжает расти. Присоединяйтесь.
        </p>
        <div class="mt-8 flex flex-wrap items-baseline gap-3 gap-y-2">
          <span id="research-count" class="research-counter-number" aria-live="polite">0</span>
          <span class="text-xl font-medium text-muted sm:text-2xl">участников</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Команда -->
  <Section id="komanda" title="Эксперты, проводящие исследование" subtitle="Мы проводим интервью, анализируем данные и готовим отраслевые отчёты." alt>
    <div class="grid gap-8 md:grid-cols-2">
      <div class="card-surface overflow-hidden reveal">
        <div class="flex flex-col sm:flex-row">
          <div class="team-photo-wrap relative sm:w-56">
            <img
              src={romanPhoto.src}
              alt="Жданов Роман"
              width={romanPhoto.width}
              height={romanPhoto.height}
            />
            <div class="absolute inset-0 ring-2 ring-inset ring-white/10 rounded-none sm:rounded-l-2xl pointer-events-none" aria-hidden="true"></div>
          </div>
          <div class="flex flex-1 flex-col p-6">
            <h3 class="text-xl font-bold text-text">Жданов Роман</h3>
            <p class="mt-1 text-sm font-medium text-muted">Интеграционный инженер</p>
            <p class="mt-4 text-sm leading-relaxed text-muted">
              Эксперт по внедрению on-prem ИИ-решений, архитектуре безопасных контуров, автоматизации процессов разработки.
            </p>
          </div>
        </div>
      </div>
      <div class="card-surface overflow-hidden reveal">
        <div class="flex flex-col sm:flex-row">
          <div class="team-photo-wrap relative sm:w-56">
            <img
              src={kristyPhoto.src}
              alt="Дементьева Кристина"
              width={kristyPhoto.width}
              height={kristyPhoto.height}
            />
            <div class="absolute inset-0 ring-2 ring-inset ring-white/10 rounded-none sm:rounded-l-2xl pointer-events-none" aria-hidden="true"></div>
          </div>
          <div class="flex flex-1 flex-col p-6">
            <h3 class="text-xl font-bold text-text">Дементьева Кристина</h3>
            <p class="mt-1 text-sm font-medium text-muted">Руководитель исследования, ИП</p>
            <p class="mt-4 text-sm leading-relaxed text-muted">
              Специалист по анализу бизнес-процессов, исследованию цифровых трендов, построению стратегий интеграции новых технологий в операционную деятельность.
            </p>
          </div>
        </div>
      </div>
    </div>
    <p class="mt-8">
      <a href={withBase("/team")} class="button button-secondary focus-ring">Подробнее о команде</a>
    </p>
  </Section>

  <!-- Аудит -->
  <section id="audit" class="section-spacing">
    <div class="container-tight">
      <div class="relative overflow-hidden rounded-2xl border border-blue/30 bg-gradient-to-br from-blue/10 via-transparent to-violet/10 p-8 shadow-2xl sm:p-12">
        <div class="glow-orb -top-20 -right-20 h-64 w-64 bg-blue/25" aria-hidden="true"></div>
        <div class="relative z-10 max-w-2xl">
          <p class="overline mb-3">Специальное предложение</p>
          <h2 class="text-2xl font-bold tracking-tight text-text sm:text-3xl">
            Углубленный аудит за символическую стоимость
          </h2>
          <p class="mt-4 text-base leading-relaxed text-muted sm:text-lg">
            Для трёх участников исследования, наиболее открытых к диалогу и представляющих стратегический интерес, мы предлагаем полномасштабный аудит возможностей автоматизации: анализ процессов, ИТ-инфраструктуры и подготовка дорожной карты.
          </p>
          <div class="mt-6 rounded-xl border border-white/10 bg-white/5 px-5 py-4">
            <p class="text-base font-semibold text-text">
              Стоимость в рамках исследования — <span class="text-blue">30 000 ₽</span>
              <span class="ml-2 text-sm font-normal text-muted">(при коммерческом заказе от 100 000 ₽)</span>
            </p>
          </div>
          <a href={withBase("/contact")} class="button button-primary mt-8 focus-ring">
            Подать заявку на аудит за 30 000 ₽
          </a>
        </div>
      </div>
    </div>
  </section>

  <script>
    (function () {
      const COUNT_URL = "https://nulnanuchittin.beget.app/webhook/recipment_count";
      const DURATION_MS = 2200;
      const EASE_OUT_QUART = (t) => 1 - Math.pow(1 - t, 4);

      const section = document.getElementById("research-results");
      const el = document.getElementById("research-count");
      if (!section || !el) return;

      let done = false;
      const observer = new IntersectionObserver(
        (entries) => {
          const entry = entries[0];
          if (!entry?.isIntersecting || done) return;
          done = true;

          fetch(COUNT_URL)
            .then((r) => r.json())
            .then((data) => {
              const target = typeof data?.count === "number" ? Math.max(0, data.count) : 0;
              const start = performance.now();

              function tick(now) {
                const elapsed = now - start;
                const p = Math.min(elapsed / DURATION_MS, 1);
                const eased = EASE_OUT_QUART(p);
                const current = Math.round(eased * target);
                el.textContent = String(current);
                if (p < 1) requestAnimationFrame(tick);
              }

              requestAnimationFrame(tick);
            })
            .catch(() => {
              el.textContent = "—";
            });
        },
        { threshold: 0.2, rootMargin: "0px 0px -60px 0px" }
      );

      observer.observe(section);
    })();
  </script>
</BaseLayout>
