---
interface Step {
  title: string;
  description: string;
}

interface Props {
  steps: Step[];
}

const { steps } = Astro.props;
---
<div class="relative">
  <div class="absolute left-4 top-0 h-full w-px timeline-line"></div>
  <div class="space-y-6">
    {steps.map((step, index) => (
      <div class="relative flex gap-5">
        <div class="mt-1 flex h-8 w-8 items-center justify-center rounded-full border border-stroke bg-bg text-sm font-semibold text-accent">
          {index + 1}
        </div>
        <div class="card-surface flex-1 p-5 reveal">
          <h3 class="text-lg font-semibold text-text">{step.title}</h3>
          <p class="mt-2 text-sm text-muted">{step.description}</p>
        </div>
      </div>
    ))}
  </div>
</div>
